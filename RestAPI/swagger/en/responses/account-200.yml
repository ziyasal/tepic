description: Account response
content:
  application/vnd.api+json:
    schema:
      type: object
      properties:
        data:
          $ref: '../schemas/index.yml#/account'
        links:
          type: object
          description: "Pagination links"
          properties:
            self:
              type: string
              description: "Link to this account object"
        meta:
          type: object
          description: "N/A"
